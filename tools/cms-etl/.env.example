# For local dev, run `npx supabase status --workdir ../../` and copy
# the "Project URL" and "Secret" values into .env (NOT here).
SUPABASE_URL=http://127.0.0.1:54421
SUPABASE_SERVICE_ROLE_KEY=<paste-the-local-service-role-key-here>

# Production values are NEVER committed. In prod, pass via environment only.
