# Vite env vars for Playwright smoke tests. Loaded via `--mode test`.
# Values are valid-shape stubs — every outbound API call is mocked by
# Playwright's page.route() interceptors in the specs.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=stub-publishable-key-for-e2e-smoke
